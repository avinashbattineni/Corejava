package Java8;

import java.util.ArrayList;
import java.util.List;

public class List6 {
    //limit example
        public static void main(String[] args) {
            List<String> ls = new ArrayList<>();
            ls.add("a");
            ls.add("d");
            ls.add("a");
            ls.add("j");
            ls.add(null);
            //ls.stream().filter(x->x!=null).filter(x->count(x)).forEach((a)->
            {
                System.out.println();
            }
        }
    }


