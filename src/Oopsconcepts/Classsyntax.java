package Oopsconcepts;

import java.util.EnumSet;

public class Classsyntax {
    //in a class extends and implements are optional we can write a class with extends and without implements //
    // with implemnets and without extends but, if we want to write both first we need to write extends then implements.
    /*[access modifire] class className [extends superclass name][implements interfaceslist]
    {
            variables
            methods
            constructors
            blocks
            Enums
            classes
            abstarct classes
            interfaces
    }
    */
}
